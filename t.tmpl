package {{ .Package }}

import "{{ .ProtoImportPath }}"

func {{ .FuncName }}(pd {{ .ProtoPackage }}.{{ .ProtoStruct }}) *{{ .StructName }} {
	return &{{ .StructName }}{
		{{- range .Fields }}
		{{ .To }}: pd.{{ .From }},
		{{- end }}
	}
}

